<div class="container">
	<div class="row">
		<h3>ジャンル追加・一覧画面</h3>

		<%= render 'admin/genres/genre_form', genre: @genre %>

	</div>

	<div class="row">
		<div class="col-xs-5">
			<table class="table">
				<thead>
					<tr>
						<th>ジャンル名</th>
						<th>状態</th>
					</tr>
				</thead>
				<tbody>
				<% @genres.each do |genre| %>
					<tr>
						<td><%= link_to edit_admin_genre_path(genre) do %>
									<%= genre.name %>
								<% end %>
						</td>
						<td><%= genre.is_valid %></td>
					</tr>
				<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>

